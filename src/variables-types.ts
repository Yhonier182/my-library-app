// Declara una variable bookTitle de tipo string y asígnale el valor "TypeScript Basics".
let bookTitle: string = "TypeScript Basics";

// Declara una constante libraryName de tipo string y asígnale el valor "My Library".
const libraryName: string = "My Library";

// Declara una variable numberOfBooks de tipo number y asígnale un valor numérico.
let numberOfBooks: number = 100;

// Declara una variable isLibraryOpen de tipo boolean y asígnale el valor true.
let isLibraryOpen: boolean = true;

// Define los tipos de datos para las siguientes variables:

// 1. titulo: El título del libro.
let titulo: string;

// 2. autor: El autor del libro.
let autor: string;

// 3. genero: El género al que pertenece el libro.
let genero: string;

// 4. idioma: El idioma en el que está escrito el libro.
let idioma: string;

// 5. precio: El precio del libro en la moneda especificada.
let precio: number;

// 6. formato: El formato en el que se vende el libro.
let formato: string;

// 7. isbn: El número de ISBN del libro para su identificación.
let isbn: string;

// 8. descripcion: Una breve descripción o sinopsis del contenido del libro.
let descripcion: string;

// 9. estado: El estado del libro.
let estado: string;

// 10. ubicacion: La ubicación donde el libro está disponible para la venta.
let ubicacion: string;

// 11. fecha_publicacion: La fecha de publicación original del libro.
let fecha_publicacion: Date;

// 12. editorial: La editorial que publicó el libro.
let editorial: string;

// 13. paginas: El número de páginas del libro.
let paginas: number;

// 14. dimensiones: Las dimensiones físicas del libro (ancho x profundidad x altura).
let dimensiones: string;

// 15. peso: El peso del libro en la unidad de medida especificada.
let peso: number;

// 16. disponible: Indica si el libro está disponible o no.
let disponible: boolean;


